const fontSizes = [12, 16, 21, 24, 36, 60, 90, '15vw'];
const space = [0, 6, 12, 24, 36, 48, 120];

const theme = {
  firetruck: {
    text: '#ffc',
    background: '#f33',
    fontSizes,
    space,
  },
  space: {
    text: '#fff',
    background: '#1a1a1a',
    fontSizes,
    space,
  }
};

export default theme;
